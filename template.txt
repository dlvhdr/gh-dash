{{ range . }}
	{{ if eq .reviewDecision \"APPROVED\" }}
		Approved
	{{ else if eq .reviewDecision \"CHANGES_REQUESTED\" }}
		Changes requested
	{{else}}
		Waiting for review
	{{end}}|
	{{truncate 35 .title}}|
	{{.headRepository.name}}|
	{{.author.login}}|
	{{timeago .updatedAt}}|
	{{.url}}
	{{ println }}
{{ end }}
{{ println }}
